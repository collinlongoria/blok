TODO (Collin)

// NEXT 2 weeks:
SVO storing material index for PBR + color (systems for blok types)
glass and refraction
shadow payload

// NEXT 2 weeks:
implement a list of lights, multiple lights
noise gen for world

// NEXT 2 weeks:
Refactor SVO rebuild into compute shader?
obj loader and voxelizer

// LONG term:
create non-pathtraced pipeline (raytraced, not using acceleration extension)
bloom?
motion blur?

// THE BIGGEST NEED:
One BLAS per chunk, multiple TLAS instances, and chunk LOD

// KNOWN ISSUES:
most of the tonemaps I tried are ugly/useless. may clean it up and remove them
no way to disable camera movement